<Window x:Class="AnimatedGifRecorder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnimatedGifRecorder" 
        xmlns:views="clr-namespace:AnimatedGifRecorder.Views"
        xmlns:converters="clr-namespace:AnimatedGifRecorder.Converters"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowState="Maximized"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.Resources>
        <ResourceDictionary>
            <converters:AppStateToCursor x:Key="AppStateToCursor"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <views:SampleToolbar HorizontalAlignment="Center"
                             VerticalAlignment="Top"
                             Panel.ZIndex="2"/>
        <views:RecordArea Cursor="{Binding State, Converter={StaticResource AppStateToCursor}}"/>
    </Grid>
</Window>
